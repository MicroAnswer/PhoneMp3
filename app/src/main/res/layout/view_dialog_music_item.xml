<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:background="?android:attr/selectableItemBackground"
  android:clickable="true"
  android:gravity="top"
  android:orientation="horizontal">

  <TextView
    android:id="@+id/view_dialog_music_item_index"
    android:layout_width="60dp"
    android:layout_height="50dp"
    android:gravity="center"
    android:paddingLeft="8dp"
    android:paddingRight="8dp"
    android:text="9999"
    android:textColor="#777777"
    android:textSize="20sp"/>


  <TextView
    android:id="@+id/view_dialog_music_item_title"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:layout_alignParentTop="true"
    android:layout_toLeftOf="@+id/view_dialog_music_item_playicon"
    android:layout_toRightOf="@+id/view_dialog_music_item_index"
    android:gravity="center_vertical"
    android:maxLines="1"
    android:textColor="@android:color/black"
    android:textSize="16sp"/>

  <answer.android.phonemp3.view.PlayAnimationIcon
    android:id="@+id/view_dialog_music_item_playicon"
    android:layout_width="wrap_content"
    android:padding="10dp"
    android:layout_height="wrap_content"
    android:layout_centerVertical="true"
    android:visibility="gone"
    android:layout_toLeftOf="@+id/view_dialog_music_item_close"/>

  <ImageView
    android:id="@+id/view_dialog_music_item_close"
    android:layout_width="60dp"
    android:layout_height="50dp"
    android:layout_alignParentRight="true"
    android:background="?android:attr/selectableItemBackground"
    android:clickable="true"
    android:padding="12dp"
    android:src="@drawable/ic_close"
    android:tint="@color/linecolor"/>

</RelativeLayout>